# key concept to rember from day 1: 27-12-2025
- need to use map to take two number input at one time
Ex. a, b = map(int, input("Enter 2 numbers space separted).split())
- while True
this keeps loop rotating automatically unless i say stop
- Python doesnt need everytime main function, so whenver we use functions,
In python its important to set run method, with main function
if __name__ == "__main__":
    main()